import { useCallback, useEffect, useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { Icon } from "../app/visual/Icon";
import { todayLocalDate } from "../domain/time";
import type { LocalDate, PlanEnrollment, ReadingProgress } from "../domain/types";
import { TodayPrayerPanel } from "../prayer/TodayPrayerPanel";
import { TodayReflectionPanel } from "../reflection/TodayReflectionPanel";
import { assignmentForCalendarDate, calendarYear, sequenceOnOrAfter, sequenceOnOrBefore } from "./calendar";
import { buildPlanReadingUrl } from "./context";
import { loadMcheynePlan } from "./loader";
import { McheyneRepository } from "./repository";
import type { AssignmentProgressSummary, McheyneAssignment, McheynePlan } from "./types";

const repository = new McheyneRepository();
function key(sequence: number, readingIndex: number): string { return `${sequence}:${readingIndex}`; }
function formatDate(localDate: LocalDate): string { const [year, month, day] = localDate.split("-").map(Number); return new Intl.DateTimeFormat(undefined, { weekday: "long", month: "long", day: "numeric" }).format(new Date(year!, month! - 1, day!, 12)); }

export function TodayScreen() {
  const today = todayLocalDate(); const [plan,setPlan]=useState<McheynePlan|null>(null); const [enrollment,setEnrollment]=useState<PlanEnrollment|null>(null); const [assignment,setAssignment]=useState<McheyneAssignment|null>(null); const [progress,setProgress]=useState<Map<string,ReadingProgress>>(new Map()); const [earlierUnread,setEarlierUnread]=useState<AssignmentProgressSummary[]>([]); const [importDate,setImportDate]=useState<LocalDate>(today); const [loading,setLoading]=useState(true); const [error,setError]=useState<string|null>(null);
  const refresh=useCallback(async()=>{setLoading(true);try{const nextPlan=await loadMcheynePlan();const nextEnrollment=await repository.getActiveEnrollment(today);setPlan(nextPlan);setEnrollment(nextEnrollment??null);if(!nextEnrollment){setAssignment(null);setProgress(new Map());setEarlierUnread([]);return;}const nextProgress=await repository.completionMap(nextEnrollment.id);setProgress(nextProgress);if(nextEnrollment.mode==="CALENDAR"){const current=assignmentForCalendarDate(nextPlan,today);setAssignment(current);const before=current?.sequence??sequenceOnOrAfter(nextPlan,today)??366;setEarlierUnread(await repository.getEarlierUnreadAssignments(nextPlan,nextEnrollment,before));}else{const sequence=await repository.getCurrentSelfPacedSequence(nextPlan,nextEnrollment);setAssignment(sequence?nextPlan.assignments[sequence-1]??null:null);setEarlierUnread([]);}setError(null);}catch(reason){setError(reason instanceof Error?reason.message:"Could not load today's reading plan.");}finally{setLoading(false);}},[today]);
  useEffect(()=>{void refresh();},[refresh]);
  const completedCount=assignment?[0,1,2,3].filter((index)=>progress.get(key(assignment.sequence,index))?.completedAt).length:0; const firstIncompleteIndex=assignment?[0,1,2,3].find((index)=>!progress.get(key(assignment.sequence,index))?.completedAt)??null:null; const unreadReadingCount=useMemo(()=>earlierUnread.reduce((sum,item)=>sum+(4-item.completedCount),0),[earlierUnread]);
  const followCalendar=async()=>{if(!plan)return;await repository.enrollCalendar(today,sequenceOnOrAfter(plan,today)??365);await refresh();}; const startSelfPaced=async()=>{await repository.enrollSelfPaced(today);await refresh();}; const importExistingProgress=async()=>{if(!plan)return;const through=sequenceOnOrBefore(plan,importDate);if(!through)return;const next=await repository.enrollCalendar(today,1);await repository.bulkImportThrough(next.id,through);await refresh();}; const toggleReading=async(readingIndex:number)=>{if(!enrollment||!assignment)return;const done=Boolean(progress.get(key(assignment.sequence,readingIndex))?.completedAt);await repository.setReadingCompleted(enrollment.id,assignment.sequence,readingIndex,!done,today);await refresh();};
  if(loading)return <main className="visual-screen today-screen"><p className="eyebrow">Today</p><p className="mcheyne-loading">Opening today’s devotion…</p></main>;
  if(error)return <main className="visual-screen today-screen"><p className="eyebrow">Today</p><h1>Today</h1><p role="alert">{error}</p></main>;
  if(!plan||!enrollment)return <main className="visual-screen today-screen"><header className="screen-heading compact-heading"><p className="eyebrow">{formatDate(today)}</p><h1>Today</h1><p className="screen-intro">Begin the M’Cheyne plan without turning earlier days into artificial debt. You can follow the calendar from today, start at Day 1, or import progress you already completed this year.</p></header><section className="mcheyne-setup" aria-labelledby="setup-heading"><p className="section-kicker">M’Cheyne reading plan</p><h2 id="setup-heading">Choose where your reading begins.</h2><div className="setup-options"><button type="button" className="primary-editorial-action" onClick={()=>void followCalendar()}><span><strong>Follow today’s calendar</strong><small>Start tracking from {formatDate(today)}. Earlier readings are not marked missed.</small></span><Icon name="arrow" /></button><button type="button" className="editorial-option" onClick={()=>void startSelfPaced()}><strong>Start self-paced at Day 1</strong><span>Move forward only when each day’s four readings are complete.</span></button></div><div className="existing-progress"><div><p className="section-kicker">Already following this year?</p><h3>Import completed-through date</h3><p>This changes plan state only. It does not fabricate devotional-history events.</p></div><div className="import-controls"><input aria-label="Completed through date" type="date" min={`${calendarYear(today)}-01-01`} max={today} value={importDate} onChange={(event)=>setImportDate(event.target.value as LocalDate)} /><button type="button" onClick={()=>void importExistingProgress()}>Import through this date</button></div></div></section><div className="today-lower-grid future-devotional-tools"><TodayReflectionPanel localDate={today} /><TodayPrayerPanel /></div></main>;
  const isLeapPause=enrollment.mode==="CALENDAR"&&assignment===null&&today.slice(5)==="02-29"; const planComplete=enrollment.mode==="SELF_PACED"&&assignment===null;
  return <main className="visual-screen today-screen"><header className="screen-heading compact-heading"><p className="eyebrow">{formatDate(today)}</p><h1>Today</h1><p className="screen-intro">{enrollment.mode==="CALENDAR"?"Today stays anchored to the historic calendar. Read ahead if useful; earlier unread passages remain available without becoming a streak or score.":"Self-paced mode follows the next incomplete M’Cheyne assignment rather than the civil calendar."}</p></header><section className="editorial-section reading-section" aria-labelledby="reading-heading"><div className="section-heading-line"><div><p className="section-kicker">M’Cheyne · {enrollment.mode==="CALENDAR"?"Calendar":"Self-paced"}</p><h2 id="reading-heading">{assignment?`Day ${assignment.sequence} readings`:isLeapPause?"Leap-day pause":"Reading plan"}</h2></div>{assignment?<span className="progress-copy">{completedCount} of 4</span>:null}</div>{isLeapPause?<div className="leap-note"><strong>No canonical M’Cheyne assignment exists for February 29.</strong><p>March 1 remains the historical March 1 assignment; the plan does not shift.</p></div>:planComplete?<div className="completion-note"><strong>All 365 M’Cheyne assignments are complete.</strong><p>Your reading history remains available in the plan and, later, in History.</p></div>:assignment?<><div className="reading-list live-reading-list">{assignment.readings.map((reading,index)=>{const done=Boolean(progress.get(key(assignment.sequence,index))?.completedAt);return <div className={`reading-row${done?" is-complete":""}`} key={`${assignment.sequence}-${index}`}><button className="reading-state reading-toggle" type="button" aria-pressed={done} aria-label={`${done?"Mark unread":"Mark complete"}: ${reading.displayReference}`} onClick={()=>void toggleReading(index)}>{done?<Icon name="check" />:<span />}</button><span className="reading-group">{reading.group==="family"?"Family":"Private"}</span><Link className="reading-passage" to={buildPlanReadingUrl(reading,enrollment.id,assignment.sequence,index,"today")}><strong>{reading.displayReference}</strong></Link><Link className="reading-arrow" aria-label={`Open ${reading.displayReference}`} to={buildPlanReadingUrl(reading,enrollment.id,assignment.sequence,index,"today")}><Icon name="arrow" /></Link></div>;})}</div>{completedCount===4?<p className="factual-completion">Today’s M’Cheyne readings are complete.</p>:firstIncompleteIndex!==null?<Link className="future-action" to={buildPlanReadingUrl(assignment.readings[firstIncompleteIndex]!,enrollment.id,assignment.sequence,firstIncompleteIndex,"today")}>Continue reading <Icon name="arrow" /></Link>:null}</>:null}<div className="plan-links-row"><Link to="/today/plan">Open full plan</Link>{earlierUnread.length>0?<span>{unreadReadingCount} earlier unread {unreadReadingCount===1?"reading":"readings"} across {earlierUnread.length} {earlierUnread.length===1?"day":"days"}</span>:<span>No earlier unread assignments in your tracked range.</span>}</div></section>{earlierUnread.length>0?<section className="earlier-unread" aria-labelledby="earlier-heading"><div><p className="section-kicker">Earlier unread</p><h2 id="earlier-heading">Available when you want them.</h2><p>These stay visible because they are unread, not because you owe a streak.</p></div><div className="earlier-list">{earlierUnread.slice(-4).map((item)=><Link key={item.assignment.sequence} to="/today/plan"><span>Day {item.assignment.sequence}</span><strong>{4-item.completedCount} unread</strong><Icon name="arrow" /></Link>)}</div></section>:null}<div className="today-lower-grid future-devotional-tools"><TodayReflectionPanel localDate={today} /><TodayPrayerPanel /></div></main>;
}
